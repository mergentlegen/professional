# Configuration

The project currently does not require external configuration.

Scripts use standard Linux utilities and can be executed
by users with appropriate permissions.

Future versions may introduce configuration files
for backup paths and monitoring thresholds.
